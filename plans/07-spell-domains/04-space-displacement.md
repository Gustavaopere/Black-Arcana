# 07.04 — Space & Displacement

## Fantasy
Controlled spatial manipulation: evasive displacement, recall, transposition, vector reversal and bounded paired thresholds without arbitrary chunk loading.

## Model
All movement resolves from authoritative server state and consumes the frozen Stage 04 displacement/destination contracts. Host integrations own presentation and invocation; Black Arcana owns safety admission and settlement.

## Implemented preparatory boundaries
- `Veilstep Reflex`: server-authoritative reactive displacement with safe-destination validation.
- `Anchor Recall`: bounded recall to a previously validated loaded anchor.
- `Reciprocal Transposition`: atomic endpoint validation and protected displacement settlement.
- `Threshold Gate`: bounded paired-threshold registry/throughput with explicit player consent and no force-loading.
- `Vector Reversal`: deduplicated bounded impulse settlement using canonical `DISPLACEMENT` admission, player/boss policy, immediate settlement revalidation and velocity clamping. Fall distance is intentionally preserved.

`Vector Reversal` checkpoint `14d73cfa9596e189a920649700b75317b2918972` passed workflow run `33317670699` (#636) in full: JUnit, diff sanity, NeoForge build, JAR inspection, GameTests and dedicated-server smoke.

## Safety invariants
- no client-authored destination/protection facts;
- no chunk tickets or force-loading;
- PvP, alliance, invulnerability and `c:bosses` policy is derived on the server;
- protection adapters are consulted through the frozen Stage 04 routes;
- technical ceilings are not Stage 08 balance values;
- unsafe or changed endpoints fail closed immediately before mutation.

## Remaining work
Host-side invocation/presentation and final balance remain downstream concerns. This preparatory checkpoint does not promote Stage 07 while Stage 05 manual QA and Stage 06 promotion remain unresolved.
