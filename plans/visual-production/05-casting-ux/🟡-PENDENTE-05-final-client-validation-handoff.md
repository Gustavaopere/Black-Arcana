# 05.05 — Final Real-Client Presentation Validation Handoff

## State

`DEFERRED VISUAL/CLIENT ACCEPTANCE`

This is the presentation half of the former mixed Stage 05 real-client campaign. Runtime/input/network/provider authority checks remain at `plans/05-casting-ux/🟡-PENDENTE-05-final-client-validation-handoff.md`.

No row here may authorize a second cast path, local gate authority, client-owned resource state or provider settlement.

## Candidate/evidence discipline

Use one exact reconciled build and record the real client/modpack context. Automated screenshots/layout tests are supporting evidence only; rows requiring direct client perception stay `PENDING`, `PASS`, `FAIL`, `BLOCKED` or `NOT APPLICABLE` according to observed evidence.

## Radial presentation

Exercise supported combinations around 854×480, 1920×1080 and 3440×1440 plus available GUI scales Auto/2/3/4. Validate:

- wedges/cards remain within the viewport;
- selection state is visually unambiguous;
- both pages of a legitimate full bounded loadout are discoverable/reachable;
- toggle/hold transitions do not leave misleading visual state;
- focus cues and pointer/keyboard modality remain readable;
- closing/selection feedback does not imply a cast occurred when it did not.

## Contextual HUD and feedback

Validate direct client presentation of:

- contextual show/hide lifecycle;
- authoritative denial detail;
- hazard/resistance threshold status;
- predictable gate state;
- stale/unavailable/provider-incompatible presentation;
- F1/hidden-GUI behavior;
- every supported HUD anchor at 0.5×/1×/2× scale with extra hazard/gate lines visible.

Wording must not claim that meeting a recommendation removes all Backlash risk, and `CLEAR` must not visually guarantee final cast success.

## Visual accessibility/client preferences

Validate the presentation effects of applicable client settings:

- MINIMAL/STANDARD/VERBOSE feedback density;
- reduced motion;
- reduced flashes;
- particle density settings;
- radial HOLD/TOGGLE presentation;
- keyboard focus/non-color focus cues;
- safe config reset/default recovery as visible to the client.

If a corresponding Black Arcana effect does not yet exist, use the project's explicit `NOT APPLICABLE / CARRIED` evidence rule instead of fabricating compliance.

## Modpack coexistence/readability

With the real installed client stack, observe:

- Black Arcana contextual HUD vs Spell Actionbar/hotbar/provider UI;
- tooltips and interactive-screen obstruction;
- recommended Black Arcana anchor/scale when automatic coexistence is not safely available;
- Epic Fight battle-mode presentation and EFIS animation interaction where a legitimate flow exists;
- no stuck cursor/key presentation after transitions;
- client readability of Black Arcana denial/result/hazard feedback while an Iron's-hosted Black Arcana action is surfaced.

Do not patch another mod's renderer through an unsupported private hook merely to improve placement.

## Visual failure triage

For a repeated visual/client FAIL, capture the exact build, viewport/GUI scale/config, reproduction sequence and observed conflict. The fix belongs in visual-production unless the evidence proves a runtime/input/data-authority defect, in which case route that defect back to the numbered Stage 05 handoff.

## Scope

This campaign owns perceptual/UI/HUD/accessibility/coexistence acceptance only. Historical mixed details remain available at `plans/visual-production/_migration-source/05-casting-ux/05-final-client-validation-handoff.pre-extraction.md`.