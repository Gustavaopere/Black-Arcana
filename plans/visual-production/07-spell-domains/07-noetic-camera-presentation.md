# 07.07 — Noetic / Astral Camera Presentation

## State

`DEFERRED VISUAL/CLIENT ACCEPTANCE / RUNTIME AUTHORITY REMAINS STAGE 07.07`

Borrowed Sight and Astral Severance session identity, loaded-only target/projection resolution, movement intent validation, termination and physical-body authority remain in `plans/07-spell-domains/`.

This file owns camera/viewpoint presentation and perceptual acceptance only.

## Borrowed Sight

The physical-client camera consumes only the authoritative server-authored target/view transition. Visual/client acceptance must verify:

- BEGIN/END transition clarity;
- camera restoration to the local physical body;
- graceful presentation when the server-authored target disappears/unloads;
- no stale viewpoint on reconnect/session reset;
- compatibility with the assembled client renderer/modpack.

The client does not choose target, ownership, duration, privacy or admission.

## Astral Severance

When the runtime path is complete, visual/client acceptance covers:

- clear start/end transition between body and astral viewpoint;
- camera/control feel without confusing physical-body position with astral viewpoint position;
- obvious restoration when projection closes, expires, exceeds range, loses representation or the connection/session resets;
- presentation that does not imply remote interaction/combat authority that the projection does not have;
- representation/model/texture/VFX/audio only after the authoritative projection identity exists.

Body vulnerability, body-damage interruption, range enforcement, loaded-only movement, logout/death/dimension cleanup and movement intent validation are runtime tests, not visual-production authority.

## Asset boundary

The final astral avatar model/texture, translucency/shader, particles/VFX, transition animation and audio are TBD. Do not derive them from Mahou assets. Reusable asset/build pipeline work belongs to `minecraft-mod-factory`.