# Non-canonical pre-extraction source snapshots

This directory preserves exact blobs of mixed planning documents as they existed at `main@cff572ea22c14b105cf7ab5345ee3b999153abd5` before visual-production extraction.

These files are **audit snapshots only**. They are not implementation authority and must not be executed as plans.

Use the active numbered engineering plans plus `plans/visual-production/` for current authority. The snapshots exist so no historical engineering or presentation requirement is silently lost while the mixed documents are split.
