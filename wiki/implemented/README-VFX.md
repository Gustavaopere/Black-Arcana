# Conteúdo visual

VFX/animação só recebe estado `IMPLEMENTADO` quando assets/render/runtime reais existirem. Direção artística em `schools/*/VFX.md` é conceito até implementação/validação real-client.
