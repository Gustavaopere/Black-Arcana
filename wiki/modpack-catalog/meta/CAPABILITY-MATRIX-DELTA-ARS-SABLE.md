# Capability Matrix Delta — Ars Sable 1.1.2

Status: `EXACT SOURCE-PINNED SPATIAL BRIDGE / ZERO SEMANTIC / CURRENT SABLE 2.0.5 QA OPEN`

| Capability | Exact 1.1.2 evidence | Black Arcana consequence |
|---|---|---|
| Ars Source visibility from moving Sable spaces | exact source contains a Source Jar bridge into Ars SourceManager with projected current position | Ars Source remains provider authority; BA must not mirror or settle Source twice |
| Moving Storage Lectern addresses | exact source tracks moved storage positions and rebuilds provider-side addressing/caches | BA must not create a competing provider storage-location ledger |
| Warp Portal destination projection | exact source adapts same/cross-dimension Ars portal coordinates through Sable spatial projection | one provider teleport settles once; BA must not issue a duplicate displacement |
| Warp Scroll / Stable Warp Scroll targets | dedicated exact-source bindings preserve targets associated with Sable spaces | provider target metadata stays provider-owned; BA spatial magic uses its own validated server-side identity |
| Planarium across Sable lifecycle | exact source contains Planarium/dimension-boundary spatial adapters | BA must not duplicate persistence for the same provider relation |
| Ars logistics/pathfinding/entities | targeted Bookwyrm, Whirlisprig, Wixie and navigation bindings exist | support is narrow; do not generalize to arbitrary entities or addons |
| Ars flying items/follow projectiles | explicit exact-source bindings exist | preserve Ars causal identity and avoid duplicate impact/proc settlement |
| Scrying/camera/rendering | common tracking plus five client mixins | client projection is presentation only; BA target validation remains server-authoritative |
| Provider-owned content | registry scaffolding exists, but exact block-item registration is empty; no provider-owned spell/glyph/ritual/action registry is established | semantic delta `+0`; no new BA school/domain is justified by provider presence |
| Provider network protocol | registrar protocol `2`; zero provider-owned payload registrations | generic forwarding helpers do not create a second gameplay authority |

## Authority boundary

Ars Nouveau remains authority for its Source, storage, warp/portal semantics, Planarium and Ars entities. Sable remains authority for its sublevel/spatial model and lifecycle. Ars Sable is the compatibility bridge between those authorities.

Black Arcana retains authority only over Black Arcana casting, hazards, Corruption, Strain, Arcane Danger, persistence and world-safety contracts. BA spatial effects must use BA's canonical server-side pipeline and `WorldEffectPolicy` where world mutation occurs, while avoiding double-processing of provider-owned actions.

## Version-risk disposition

The source declares Sable range `[1.0,)`, but its build pin is `1.2.2` and the physical runtime is `2.0.5`. With 29 required direct mixin bindings, loader-range acceptance is neither a runtime PASS nor an inferred failure. Current-host behavior remains fail-closed pending direct QA.
