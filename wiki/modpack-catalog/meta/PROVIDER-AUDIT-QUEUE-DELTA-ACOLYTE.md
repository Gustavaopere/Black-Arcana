# Provider Audit Queue Delta — Acolyte 1.0.3

## State

`EXACT PUBLISHER-RELEASE STRUCTURAL AUDIT / ZERO_SEMANTIC_SPELLCASTER_CONTENT / PHYSICAL HASH EQUALITY OPEN`

This narrow overlay applies to `acolyte` until the current 22/09 physical-provider queue is regenerated.

| Mod ID | JAR | Exact state | Semantic delta | Component effect |
|---|---|---|---:|---|
| `acolyte` | `acolyte-1.0.3.jar` | sibling physical identity #4; exact CurseForge file `8098233`; 93 provider classes; 0 `AbstractSpell` subclass chain; 0 provider spell resources; 0 provider spell localization roots; 31 explicit Iron's host spell references plus dynamic host selection | +0 | cataloged as magic/spellcaster component; current global component denominator remains pending rebase |

## Exact publisher evidence

- CurseForge project: `1534642`;
- exact release file: `8098233`;
- publisher filename: `acolyte-1.0.3.jar`;
- publisher SHA-1: `ad0abc821f3f8a6573ac19a50970fc1bc46b4275`;
- publisher SHA-256: `73fe745bbb0016fbc00af9ba14c0ecfe0a1d8a7d72a7892557739182b3486256`;
- temporary exact-release audit run: GitHub Actions `35807694901`, job `107012065606`, SUCCESS.

The sibling physical record does not currently provide an independent local SHA, so exact physical↔publisher byte equality remains open.

## Closed by structural evidence

- no provider-owned `AbstractSpell` class chain;
- no provider-owned spell resource path;
- no provider-owned spell localization identity;
- Acolyte uses Iron's spellcasting mob/API surfaces;
- 29 explicit Iron's spell IDs occur in packaged provider resources;
- two additional host spells are referenced by `SpellRegistry` fields: Blood Step and Stomp;
- dynamic `getSpell` / random-spell selection exists, so fixed per-archetype loadouts are not inferred;
- semantic classification: **+0**.

## Still fail-closed

- independent hash of the installed physical JAR;
- exact assembled-pack runtime boot/config;
- per-archetype active pools, spell levels and weights;
- biome-variant differences;
- Horn Merchant live trade table;
- recruitment persistence and ownership;
- multiplayer/protection/duplicate-processing behavior;
- any Black Arcana runtime adapter.

## Metric rule

Acolyte is a newly reconciled current magic component discovered during the 22/09 physical rebase. It does not change the strict semantic numerator because all observed spell identities belong to Iron's.

The old fixed technical denominator is already stale under the current physical re-audit; this overlay does not publish a replacement fraction.
