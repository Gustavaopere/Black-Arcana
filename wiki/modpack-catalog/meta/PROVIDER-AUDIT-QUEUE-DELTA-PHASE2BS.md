# Provider audit queue delta — Phase 2BS

## Candidate

**T.O Magic n' Extras / `traveloptics` 4.4.0.1-1.21.1**

Physical version-line presence is confirmed. Exact publisher file `6342780` was materialized under clean-room constraints and hash-gated at SHA-1 `3808493ce45cdfeb6408e85578adecf13df698e8`.

## Disposition

**⚠️ PARTIAL / CONDITIONED — exact registry cataloged, strict promotion blocked.**

Phase 2BS closes:

- 33 exact registered spell identities;
- 33 field -> concrete class -> registry-ID mappings;
- zero registry initializer branches;
- 32 stale/residual root localization spell IDs as metric-excluded;
- provider crafting-gate constants for `AbstractUniqueSpell` and `AbstractWeaponSpell`;
- nine direct loot routes for non-craftable Unique spells;
- exact structural `TOLootModifiers` codec-reference facts.

Phase 2BS does **not** close:

- object-level survival reachability for `traveloptics:blackout`;
- authoritative runtime viability of the exact physical T.O alpha;
- proof that the physical pack includes a third-party compatibility patch/replacement;
- Somake Aqua ↔ T.O Aqua current authority/coexistence behavior.

## Metric effect

- semantic delta: **+0 strict**;
- technical component delta: **+0**;
- current strict semantic minimum stays **1344**;
- current technical component closure stays **66/100**;
- component #67 remains open.

The 33 exact registry identities are an inventory fact, not a strict counted semantic contribution while these gates remain open.

## Blocking evidence

### Blackout reachability

`AbstractUniqueSpell.allowCrafting() = false`. `traveloptics:blackout` is a Unique registration with:

- no exact structured acquisition reference found;
- no provider-owned reference to `TOSpells.BLACKOUT_SPELL` outside the registry class found by focused audit.

The publisher's generic survival-obtainability statement is insufficient to infer a Blackout-specific route.

### Runtime registry risk

Exact `TOLootModifiers` structure contains both `key_loot` and `universal_loot` registry names but references `KeyLootModifier.CODEC` twice and `UniversalLootModifier.CODEC` zero times.

A later third-party patch reports that this wiring prevents registry startup and changes the universal entry, but the third-party diagnosis is not upstream authority and no crash is claimed as reproduced by Black Arcana.

Promotion remains fail-closed until physical/runtime evidence resolves this risk.

## Audit anchors

- run `34740821956` / artifact `10312358984` — exact artifact structure;
- run `34740904391` / artifact `10312645516` — focused registry closure;
- run `34741045570` / artifact `10311499946` — 33/33 field/class/ID semantic reconciliation;
- run `34741368134` / artifact `10312730434` — corrected crafting-gate + runtime-risk reconciliation.

## Queue consequence

Do not select T.O Magic as component #67 until its blockers close. Keep it in the audited-partial set alongside other fail-closed providers rather than repeatedly restarting its registry reconstruction.

The next provider-selection pass should move to another genuinely open physical candidate while T.O remains parked, unless new physical/runtime evidence specifically resolves its blockers.
