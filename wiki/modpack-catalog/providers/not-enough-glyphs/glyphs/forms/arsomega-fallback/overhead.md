# Overhead

- Registry: `arsomega:overhead`
- Class: NEG `MethodOverhead`
- Active because `arsomega` is absent.
- Default mana: **5**; tier inherited/default where not separately overridden.
- Semantics: targets/resolves at a block above the caster.
- Acquisition: Iron Helmet + Air Essence.
- Boundary: external targeting form.