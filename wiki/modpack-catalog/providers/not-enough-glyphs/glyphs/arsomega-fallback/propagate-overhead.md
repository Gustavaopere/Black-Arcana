# Propagate Overhead

- Registry: `arsomega:propagate_overhead`
- Class: NEG `PropagateOverhead`
- Active because `arsomega` is absent.
- Tier: **II**; default mana: **100**
- Semantics: resolves the remainder overhead from the propagation point.
- Acquisition: Manipulation Essence + Overhead glyph.
- Boundary: external propagation coverage.