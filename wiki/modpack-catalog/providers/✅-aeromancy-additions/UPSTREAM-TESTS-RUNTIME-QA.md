# SnackPirate's Aeromancy Additions 1.2.8 — runtime QA

Status: `SOURCE INVENTORIED / CURRENT PHYSICAL HOST QA NOT EXECUTED`

The exact source tree has no `src/test` subtree, so no upstream automated PASS is claimed.

Current-pack validation remains open for client and dedicated-server boot, both required mixins, Iron's 3.16.3, NeoForge 21.1.248, Wind focus resolution in Scroll Forge, acquisition and representative execution of all ten registered spells, embedded-spell items, provider synchronization, persistence/reload and multiplayer duplicate-processing behavior.

The provider-declared Iron's range includes the physical 3.16.3 version. That establishes only declared compatibility, not runtime PASS.

Fail-closed rule: if assembled-pack behavior contradicts the source-pinned contract, Black Arcana withholds the affected optional integration and does not invent provider state or duplicate provider/host settlement.
