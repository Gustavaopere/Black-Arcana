name: Somake 1.0.9 Registry Audit NON-MERGE

on:
  push:
    branches:
      - 'feat/audit-somake-1.0.9-registry-non-merge-2026-09-25'

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Exact Somake 1.0.9 registry structural audit NON-MERGE
        run: python3 .github/scripts/tmp_somake_1_0_9_registry_audit.py
