name: Temporary Provider Artifact Audit

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  audit:
    if: github.head_ref == 'docs/provider-artifact-audit-alshanex-4.0.3'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download exact Alshanex 4.0.3 artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p audit
          URL='https://cursemaven.com/curse/maven/alshanexs-familiars-1171602/8675568/alshanexs-familiars-1171602-8675568.jar'
          curl --fail --location --retry 3 --retry-delay 2 "$URL" -o /tmp/provider.jar
          ACTUAL="$(sha1sum /tmp/provider.jar | awk '{print $1}')"
          EXPECTED='e5051c2385a426d05bf203ba8081a23d891f6686'
          printf 'expected_sha1=%s\nactual_sha1=%s\n' "$EXPECTED" "$ACTUAL" | tee audit/hash.txt
          test "$ACTUAL" = "$EXPECTED"

      - name: Extract factual resource inventory only
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from zipfile import ZipFile

          jar = '/tmp/provider.jar'
          report = []
          with ZipFile(jar) as z:
              names = sorted(z.namelist())
              lower = {n: n.lower() for n in names}

              report.append('EXACT JAR RESOURCE INVENTORY — Alshanex Familiars 4.0.3')
              report.append('No class implementation is decompiled; this section inspects archive paths and structured resources only.')
              report.append('')

              report.append('[exact path hits: switcheroo / fire_fist]')
              hits = [n for n in names if 'switcheroo' in lower[n] or 'fire_fist' in lower[n] or 'firefist' in lower[n]]
              report.extend(hits or ['<none>'])
              report.append('')

              report.append('[provider class filenames containing Spell or Ritual]')
              class_hits = [n for n in names if n.endswith('.class') and n.startswith('net/alshanex/') and ('spell' in lower[n] or 'ritual' in lower[n])]
              report.extend(class_hits or ['<none>'])
              report.append('')

              report.append('[provider data JSON paths containing ritual]')
              ritual_paths = [n for n in names if n.endswith('.json') and n.startswith('data/alshanex_familiars/') and 'ritual' in lower[n]]
              report.extend(ritual_paths or ['<none>'])
              report.append('')

              report.append('[ritual_recipe JSON facts: path | type | result-id]')
              ritual_facts = []
              for n in names:
                  if not (n.endswith('.json') and n.startswith('data/alshanex_familiars/')):
                      continue
                  try:
                      obj = json.loads(z.read(n))
                  except Exception:
                      continue
                  if not isinstance(obj, dict) or obj.get('type') != 'alshanex_familiars:ritual_recipe':
                      continue
                  result = obj.get('result')
                  if isinstance(result, dict):
                      result = result.get('id') or result.get('item') or result.get('name')
                  ritual_facts.append((n, obj.get('type'), result))
              for n, typ, result in ritual_facts:
                  report.append(f'{n} | {typ} | {result}')
              if not ritual_facts:
                  report.append('<none>')
              report.append(f'ritual_recipe_count={len(ritual_facts)}')
              report.append('')

              report.append('[provider-owned spell translation keys]')
              base_spell_keys = []
              lang_path = 'assets/alshanex_familiars/lang/en_us.json'
              if lang_path in names:
                  obj = json.loads(z.read(lang_path))
                  for key in obj:
                      if key.startswith('spell.alshanex_familiars.') and not key.endswith('.guide'):
                          base_spell_keys.append(key)
              report.extend(sorted(set(base_spell_keys)) or ['<none>'])
              report.append(f'provider_spell_translation_count={len(set(base_spell_keys))}')

          text = '\n'.join(report) + '\n'
          print(text)
          with open('audit/resource-report.txt', 'w', encoding='utf-8') as f:
              f.write(text)
          PY

      - name: Inspect registry member signatures without bytecode disassembly
        shell: bash
        run: |
          set -euo pipefail
          {
            echo 'EXACT JAR MEMBER SIGNATURES — PetSpellRegistry'
            echo 'javap -p only: declarations/signatures; no -c bytecode disassembly and no source decompiler.'
            echo
            javap -classpath /tmp/provider.jar -p net.alshanex.alshanex_familiars.registry.PetSpellRegistry
          } | tee audit/pet-spell-registry-signatures.txt

      - name: Map spell classes to literal provider ids without disassembly
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import re
          from zipfile import ZipFile

          candidates = {
              'end_mayhem', 'fire_fist', 'ice_age', 'ice_chamber',
              'megido', 'summon_shadows', 'switcheroo'
          }
          rows = []
          with ZipFile('/tmp/provider.jar') as z:
              classes = sorted(n for n in z.namelist() if n.startswith('net/alshanex/alshanex_familiars/spells/') and n.endswith('.class'))
              for path in classes:
                  data = z.read(path)
                  literals = set()
                  for match in re.findall(rb'[ -~]{4,}', data):
                      text = match.decode('ascii', 'ignore')
                      for token in candidates:
                          if token in text:
                              literals.add(token)
                  if literals:
                      rows.append((path, ','.join(sorted(literals))))
          print('EXACT CLASS LITERAL-ID MAP — filtered provider spell ids only')
          print('No bytecode instructions or implementation bodies are decoded.')
          for path, ids in rows:
              print(f'{path} | {ids}')
          print(f'mapped_class_count={len(rows)}')
          with open('audit/spell-literal-id-map.txt', 'w', encoding='utf-8') as f:
              f.write('EXACT CLASS LITERAL-ID MAP — filtered provider spell ids only\n')
              f.write('No bytecode instructions or implementation bodies are decoded.\n')
              for path, ids in rows:
                  f.write(f'{path} | {ids}\n')
              f.write(f'mapped_class_count={len(rows)}\n')
          PY

      - name: Publish text-only audit report
        uses: actions/upload-artifact@v7
        with:
          name: alshanex-4.0.3-resource-audit
          path: audit/*.txt
          if-no-files-found: error
          retention-days: 3
